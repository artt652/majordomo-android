# Redis конфиг для Termux/Android
# RDB снапшоты отключены — Android ограничивает запись файлов снапшотов
# Без этого Redis падает с ошибкой MISCONF при попытке сохранить данные на диск

save ""
stop-writes-on-bgsave-error no
appendonly no
loglevel warning
